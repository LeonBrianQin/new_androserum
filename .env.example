# ----------------------------------------------------------------
# Copy this file to `.env.local` and fill in real values.
# `.env.local` is gitignored; this `.env.example` is committed as a
# template only. Load it before running the pipeline:
#
#     cp .env.example .env.local
#     vim .env.local      # paste your real keys
#     set -a; source .env.local; set +a
#
# ----------------------------------------------------------------

# AndroZoo API key. Apply at https://androzoo.uni.lu/access (free
# for academic use). Required for `scripts/00_download_apks.py`.
ANDROZOO_APIKEY=
